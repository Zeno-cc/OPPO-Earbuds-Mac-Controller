# v1.5.1 更新体验

基于请求时最新 main d967cdc（v1.4.0），不补做未合入的 v1.5 耳机功能。

详情 → 关于增加紧凑软件更新区：动态版本、状态说明、成功检查时间、检查更新按钮、自动检查开关和官方发布入口。沿用系统字体、语义颜色和轻量背景，不使用模拟进度或耳机 HUD。

发现新版本后的说明、下载、验证、安装、授权和 Relaunch 由 Sparkle 标准界面负责。用户主动检查时关闭 Popover 并激活应用；后台没有更新或断网不主动激活。安装前提醒保存 EQ 草稿，持久化设置不会被主动清除。

默认 12 小时检查，可关闭；用户确认后才下载和安装，不强制静默重启。固定 stable feed，无自制版本比较、下载器、提权服务或安装器。

暂无可安装的更新并不保证服务器没有更高版本；系统或架构不兼容也可能没有候选。解压后仍显示验证，不提前宣称已安装。

Debug 缺少正式公钥时明确显示未配置并禁用更新。Release 打包缺 key 失败。一次性测试密钥不能公开发行。

软件测试、真实 Sparkle 安装器替换、图形窗口与 Relaunch、蓝牙/TCC 是不同验证层次，不互相冒充。协议、ANC、EQ 业务逻辑不在本版本改动范围内。
